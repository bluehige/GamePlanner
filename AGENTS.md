# GamePlanner Repository Rules

## 목적

이 저장소는 게임 제작 전 과정을 통제하는 재사용 Agent Skill 모음이다. 설명문보다 실행 절차, 하드 게이트, 산출물, 증거 규칙을 우선한다.

## 우선순위

1. 이 `AGENTS.md`
2. 각 `skills/<number>-*/SKILL.md`
3. 프로젝트별 게임 계약과 `.agents/skills/<game>-foundation/SKILL.md`
4. 활성 장르·엔진·플랫폼 규칙
5. 외부 스킬 원본

프로젝트 저장소에 더 구체적인 규칙이 있으면 그 규칙이 우선한다. 단, 완료 증거와 안전한 변경 범위를 약화해서는 안 된다.

## 편집 규칙

- 번호 `00`~`18`, `90`은 공개 API다. 의미를 바꾸거나 재사용하지 않는다.
- 새 스킬은 frontmatter에 `name`, `description`을 포함한다.
- 모든 스킬은 `사용 시점`, `입력`, `절차`, `산출물`, `완료 게이트` 섹션을 가진다.
- 외부 스킬의 실질적 원문을 복사하지 않는다. 연결 정보는 `external/UPSTREAMS.lock.json`에 고정한다.
- 외부 버전을 갱신할 때는 변경 내용을 읽고 어댑터 호환성을 다시 검토한다.
- 검증 없이 `완료`, `수정됨`, `출시 가능`을 선언하지 않는다.
- 스킬 변경 후 `python scripts/validate_repository.py`와 전체 테스트를 실행한다.
- 샘플 파일에 토큰, 개인 데이터, 비공개 저장소 내용, 저작권이 불명확한 자산을 넣지 않는다.

## 문서 언어

본문은 한국어를 기본으로 한다. frontmatter의 설명은 자동 탐색을 위해 명확한 영어 문장으로 작성할 수 있다.

## 외부 의존성 실패

외부 스킬이 설치되지 않았거나 접근할 수 없으면:

1. 실패 사실과 누락된 기능을 명시한다.
2. 해당 단계의 GamePlanner 내부 최소 절차를 수행한다.
3. 외부 스킬을 사용한 것처럼 주장하지 않는다.
4. 결과의 증거 수준을 낮춰 기록한다.
